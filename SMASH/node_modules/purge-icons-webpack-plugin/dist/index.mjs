var __assign = Object.assign;

// src/index.ts
import {PurgeIcons as Purge} from "@purge-icons/core";
import VirtualModulesPlugin from "webpack-virtual-modules";
var modulePath = "node_modules/@purge-icons/generated";
var PurgeIcons = class {
  constructor(options = {}) {
    this.options = options;
    this.plugin = new VirtualModulesPlugin({[modulePath]: ""});
  }
  apply(compiler) {
    this.plugin.apply(compiler);
    Purge(__assign(__assign({}, this.options), {
      format: "cjs"
    })).then((code) => {
      this.plugin.writeModule(modulePath, code);
    });
  }
};
var src_default = PurgeIcons;
export {
  PurgeIcons,
  src_default as default
};
